"""M6 Sprint 12 fixture-build pipeline (public-source anchors → v0.2.0 envelopes).

stdlib only, no network. Modules: normalize (GDR-016 single shared path), trim,
manifest, split, variants, build (orchestrator). Governs data/fixtures/ outputs;
raw payloads stay under the git-ignored data/raw/.
"""
