[pytest]
# Leadership decision #5: standard library plus pinned pytest only. `pythonpath`
# and `testpaths` are pytest configuration, not additional dependencies.
pythonpath = src
testpaths = tests
addopts = -ra
